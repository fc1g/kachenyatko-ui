export const REVALIDATE_PRODUCT = 60;

export enum NUM_OF_PRODUCTS {
  NEWEST = 2,
  BESTSELLER = 3,
  OTHER = 4,
}

export enum PRODUCTS_PAGINATION {
  LIMIT = 6,
  PAGE = 1,
  NAV_LIMIT = 3,
  SORT = 'ASC',
}
